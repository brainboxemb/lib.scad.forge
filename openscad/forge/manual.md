# Forge overview

This path is retained as a compatibility pointer.

Forge API/reference documentation is now source-driven. Start from the
[repository README](../../README.md) or the owning `.scad` source files.
Generate the browseable reference with `scripts/build-api-docs.sh`.
