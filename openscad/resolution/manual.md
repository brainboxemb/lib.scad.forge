# Geometry resolution

This path is retained as a compatibility pointer.

Forge API/reference documentation is now source-driven. The authority for this
topic is [`resolution.scad`](../resolution.scad). Generate the browseable reference with
`scripts/build-api-docs.sh` from the repository root.
